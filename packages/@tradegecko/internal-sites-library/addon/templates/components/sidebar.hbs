<aside class="">
  <img src="/logo-mark.svg" width="50" height="50">
  {{#each-in this.navWithChildren as | index nav |}}
    {{log nav}}
    <h4 class="title">
      {{nav.title}}
    </h4>
    <ul>
      {{#each nav.pages as |child|}}
        <li>
          {{#link-to "page" child.url class="link"}}
            {{child.title}}
          {{/link-to}}
        </li>
      {{/each}}
      {{#each-in nav.children as |index childNav|}}
          <li class="list-item">
            <details>
              <summary class="details-title">
                {{childNav.title}}
              </summary>
              <ul>
                {{#each-in childNav.pages as |index child|}}
                  <li>
                    {{#link-to "page" child.url class="link"}}
                      {{child.title}}
                    {{/link-to}}
                  </li>
                {{/each-in}}
              </ul>
            </details>
          </li>
        {{/each-in}}
    </ul>
  {{/each-in}}
</aside>
